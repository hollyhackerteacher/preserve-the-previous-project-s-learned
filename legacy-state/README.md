# Legacy learned map artifacts

These files are read-only exports from the original A-Maze-ing project for
offline inspection and migration. They contain learned planner snapshots,
legacy map evidence, event journals, and active-layer evidence.

Operational control artifacts were intentionally excluded: STOP flags, locks,
supervisor/run status, and live session state.

The SQLite database is tracked with Git LFS.
